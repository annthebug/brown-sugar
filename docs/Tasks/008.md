# Task 008 — MBTI 系統（題庫與計分）

狀態：✅ 完成

## 目標
建立隱藏式 MBTI 題庫與計分邏輯。

## 範圍
- 題庫（16~24 題，分散五章）。
- 四維計分（E/I、S/N、T/F、J/P）。
- 完成後計算結果並對應玻璃碗。

## 對應文件
- [07_MBTISystem](../07_MBTISystem.md)

## 實作步驟
1. 建立 `data/mbti.ts` 題庫。
2. 建立 `services/mbti.ts` 計分服務與 `useMbtiStore`。
3. 對話選項串接計分。
4. 建立 MBTI → 玻璃碗對照表。

## 驗收標準
- 完成所有題目可得出正確四碼結果。
- 結果對應到玻璃碗。

## 相依任務
- 007

## 完成紀錄（2026-07-09）

### 已實作
- `src/data/mbti.ts`：21 題，分散五章（Forest 5、City 5、Snow 4、Glass 4、Retry/Final 3）；`getQuestionById`、`getQuestionsByChapter`、`MBTI_QUESTION_COUNT`。
- `src/services/mbti.ts`：`calculateMbtiType()`、`MBTI_BOWL_NAMES`、`getBowlIdForType()`、`getBowlSheetBackgroundPosition()`；16 型與 `mbti-glass-bowls-sheet` 4×4 網格對照。
- `src/stores/useMbtiStore.ts`：四維分數、`answeredQuestionIds`、`answerQuestion(questionId, preference)` 防重複計分、`isComplete()`、`getMbtiResult()`、LocalStorage persist。
- `src/data/dialogues.ts`：各章 NPC 腳本（`forestElder`、`cityBarista`、`cityTraveler`、`snowSpirit`、`glassMaster`、`innerGuide`）以 `questionId` 串接題庫；選項帶 `kind: 'mbti'`。
- `src/routes/GamePage.tsx`：`handleChoiceResult` 呼叫 `answerQuestion`；顯示答題進度。
- `src/routes/EndingPage.tsx`：答完 21 題後顯示四碼 MBTI、碗名與 sprite sheet 裁切預覽。

### 遊戲內觸發範圍
- **場景內 NPC（正式流程）：** Forest 5 題（森林老人）、City 5 題（咖啡店店員、公園旅人）可於 `ForestScene` / `CityScene` 靠近按 E 觸發。
- **Debug 觸發：** `GamePage` 面板可按鈕開啟 `snowSpirit`、`glassMaster`、`innerGuide` 對話，供第三至五章題目測試；待 Task 011–014 實作場景後改為場景內 NPC 觸發。

### 備註
- 題數 21 題，符合文件建議的 16~24 題範圍。
- `GamePage` debug 面板仍保留，方便開發測試；不影響 MBTI 系統驗收。
