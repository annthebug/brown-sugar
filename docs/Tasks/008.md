# Task 008 — MBTI 系統（題庫與計分）

狀態：🟨 進行中

## 目標
建立隱藏式 MBTI 題庫與計分邏輯。

## 範圍
- 題庫（16~24 題，分散五章）。
- 四維計分（E/I、S/N、T/F、J/P）。
- 完成後計算結果並對應玻璃碗。

## 對應文件
- [07_MBTISystem](../07_MBTISystem.md)

## 實作步驟
1. 建立 `data/mbti.ts` 題庫。
2. 建立 `services/mbti.ts` 計分服務與 `useMbtiStore`。
3. 對話選項串接計分。
4. 建立 MBTI → 玻璃碗對照表。

## 驗收標準
- 完成所有題目可得出正確四碼結果。
- 結果對應到玻璃碗。

## 相依任務
- 007

## 目前盤點（2026-07-09）

### 已實作
- `src/stores/useMbtiStore.ts`：四維分數（EI / SN / TF / JP）、`answerQuestion` 加減分、LocalStorage persist（`perfect-bowl-mbti`）、`resetScores`。
- `src/data/dialogues.ts`：`forestElder` 腳本含 2 個 MBTI 選項（EI 維度），選項帶 `kind: 'mbti'` 與 `dimension` / `preference`。
- `src/components/DialogueBox.tsx`：選項時回傳 `onChoiceResult`。
- `src/routes/GamePage.tsx`：`handleChoiceResult` 串接 `answerQuestion`；可按 E 或按鈕開啟 Forest Elder 對話；面板顯示 EI 分數與最後選項。
- `assetManifest.ts` 已預載 `mbtiGlassBowls` placeholder；設計文件 `docs/assets/mbti-glass-bowls.md` 定義 4×4 網格順序。

### 尚未實作
- `src/data/mbti.ts` 題庫（16–24 題、五章分散）**不存在**。
- `src/services/mbti.ts` 計分服務 **不存在**（`src/services/` 目錄尚未建立）。
- 四維分數 → 四碼 MBTI 類型計算邏輯 **無**。
- MBTI → 玻璃碗對照表與取用邏輯 **無**（`EndingPage` 僅顯示原始分數，未對應碗型）。
- SN / TF / JP 維度尚無對話題目資料；目前僅 Forest Elder 一題 EI。
- 五章隱藏式題目流程尚未展開，僅有單段測試對話可計分。
