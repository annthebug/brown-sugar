# Task 009 — 關卡：第一章 Forest

狀態：✅ 完成

## 目標
實作第一章森林關卡。

## 範圍
- 場景：森林 tilemap，背景維持淡藍色天空與低飽和莫蘭迪綠意。
- 怪物：毛線球、紙箱、小魚乾。
- Boss：巨大罐罐。
- 完成後解鎖 Memory #1（第一次一起出去）。
- 融入 E/I、S/N 相關 MBTI 對話。

## 對應文件
- [02_GameDesign](../02_GameDesign.md)
- [04_WorldBuilding](../04_WorldBuilding.md)

## 實作步驟
1. 建立 ForestScene 與 tilemap。
2. 放置怪物與 Boss。
3. 佈置 NPC（森林老人）對話與題目。
4. 串接 Memory #1 解鎖。

## 驗收標準
- 可通關第一章並解鎖 Memory #1。
- MBTI 題目正確計分。
- ForestScene 的天空、遠景、地表與 UI 元素符合淡藍天空與莫蘭迪色系規範。

## 相依任務
- 005, 006, 007, 008

## 完成紀錄（2026-07-09）

### 已實作
- `src/game/scenes/ForestScene.ts`：橫向卷軸森林關卡（`WORLD_WIDTH` 2520）；淡藍天空 `tileSprite`、樹影剪影、雲朵、鼠尾草綠地面與多層平台（placeholder 幾何矩形，非 Tiled 匯入 tilemap）。
- `src/game/entities/Player.ts` + `InputController` + `TouchControlsOverlay`：完整操作整合；相機跟隨玩家。
- `src/game/entities/MemoryShard.ts`：8 個 shard 以 `physics.add.overlap` 碰撞拾取，呼叫 `player.triggerCollect()` 並 emit `memory-shard-collected`。
- `src/game/entities/PatrolCritter.ts`：毛線球（`yarnBall`）× 2、紙箱（`cardboardBox`）× 1，緩慢巡邏、碰到玩家輕微擊退（`bumpPlayer`），不扣血。
- **森林老人 NPC：** `placeForestElder()` 放置於平台區；靠近按 E 觸發 `player.triggerTalk('forestElder')` → `DialogueBox` 播放 5 題 E/I、S/N MBTI 對話。
- **Boss 巨大罐罐：** `placeGiantJarBoss()`；靠近按 E 互動通關（非暴力）；emit `chapter:forest-cleared`；2.2 秒後 `scene.start('CityScene')`。
- `src/routes/GamePage.tsx`：監聽 `chapter:forest-cleared` → `completeForestChapter()` + `unlockMemoryByNumber(1)` + `MemoryOverlay` 播放。
- `src/stores/useGameStore.ts`：`forestChapterCleared` 旗標 persist；`chapters.ts` 依通關狀態決定 Continue 進入章節。

### 與文件範圍的差異（設計取捨）
- **小魚乾怪物：** 本版僅實作毛線球與紙箱兩種 `PatrolCritterKind`；第三種列於 Out of Scope。
- **Tilemap：** 採 placeholder 矩形平台（設計取捨，不匯入 Tiled tilemap）。
- **`GameScene` 仍保留**於 Phaser config 作開發測試場景，正式流程以 `ForestScene` 為第一章入口。

### 驗收對照
| 驗收項目 | 狀態 |
| --- | --- |
| 可通關第一章 | ✅ 巨大罐罐互動後進入 City |
| 解鎖 Memory #1 | ✅ `unlockMemoryByNumber(1)` |
| MBTI 題目計分 | ✅ 森林老人 5 題串接 `useMbtiStore` |
| 淡藍天空 + 莫蘭迪色系 | ✅ `MORANDI_PALETTE` 全場景套用 |
