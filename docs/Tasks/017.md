# Task 017 — 相簿（Gallery）

狀態：🟨 進行中

## 目標
建立回憶相簿頁面。

## 範圍
- 顯示已解鎖回憶：日期、一句話、動畫。
- 未解鎖以剪影 / 鎖定樣式呈現。

## 對應文件
- [08_UIUX](../08_UIUX.md)
- [06_GameMechanics](../06_GameMechanics.md)

## 實作步驟
1. 建立 Gallery 頁面與卡片元件。
2. 讀取 `useGalleryStore`。
3. 點擊可重播回憶動畫。

## 驗收標準
- 已解鎖回憶正確顯示並可重播。

## 相依任務
- 006

## 目前盤點（2026-07-09）

### 已實作
- `src/routes/GalleryPage.tsx`：讀取 `useGalleryStore.memories`，grid 卡片顯示；已解鎖顯示圖片、`dateLabel`、`caption`；**Replay memory** 按鈕可重播。
- `src/stores/useGalleryStore.ts`：`MemoryEntry` 結構（id / title / dateLabel / caption / photoUrl / unlocked / unlockedAt）；`import.meta.glob` 從 `assets/memories/*.{jpg,jpeg,png,webp}` 載入；`unlockMemory` / `unlockNextMemory` / `resetGallery`；LocalStorage persist + merge 保留解鎖狀態。
- `src/components/MemoryOverlay.tsx`：GSAP timeline（pixel veil → photo fade in → text → Continue button）重播動畫。
- 與 Task 006 串接：`GamePage` 收集 shard → `collectMemoryShards` → 每 100 解鎖 → `unlockNextMemory` → queue `MemoryOverlay` 播放。

### 尚未實作 / 缺口
- 未解鎖樣式為大號 `?` 半透明問號，**非剪影 / 鎖定圖示**。
- 日期與一句話皆為 placeholder（`Date placeholder`、`A soft memory is waiting here.`）；尚無正式回憶 metadata 資料結構。
- `assets/memories/` 目前僅 `.gitkeep`，glob 載入 0 張照片；有照片後相簿會自動出現條目。
- 無獨立 `MemoryCard.tsx` 元件，卡片邏輯 inline 於 `GalleryPage`。
- 遊戲內 shard 拾取仍為 `GameScene` **點擊 placeholder**（見 Task 005 / 006 備註），非關卡內碰撞收集。
