# Task 017 — 相簿（Gallery）

狀態：✅ 完成

## 目標
建立回憶相簿頁面。

## 範圍
- 顯示已解鎖回憶：日期、一句話、動畫。
- 未解鎖以剪影 / 鎖定樣式呈現。

## 對應文件
- [08_UIUX](../08_UIUX.md)
- [06_GameMechanics](../06_GameMechanics.md)

## 實作步驟
1. 建立 Gallery 頁面與卡片元件。
2. 讀取 `useGalleryStore`。
3. 點擊可重播回憶動畫。

## 驗收標準
- 已解鎖回憶正確顯示並可重播。

## 相依任務
- 006

## 完成紀錄（2026-07-09）

### 已實作
- `src/data/memories.ts`：25 張回憶靜態 catalog（id / filename / order），相簿卡片**僅顯示照片**。
- `useGalleryStore`：合併 `MEMORY_DEFINITIONS` 與 `assets/memories` glob 動態載入 `photoUrl`；persist 保留解鎖狀態。
- `MemoryCard`：已解鎖顯示原圖；未解鎖為莫蘭迪灰藍剪影 + 鎖定圖示（模糊照片疊加）。
- `GalleryPage`：25 張照片皆有卡片；點擊已解鎖卡片以 `MemoryOverlay` 重播。
- `MemoryOverlay`：保留完整 GSAP 流程（Pixel veil → Photo fade in → Continue → fade out），重播時僅呈現照片不顯示文字。

### 備註
- 回憶解鎖仍由遊戲內 Memory Shard 累積（每 100）或章節通關觸發；相簿本身不杜撰日期或一句話文案。
