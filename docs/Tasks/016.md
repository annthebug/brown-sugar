# Task 016 — UI：首頁 / 暫停 / 設定

狀態：🟨 進行中

## 目標
建立主要選單 UI。

## 範圍
- 首頁：Start、Continue、Gallery、Setting。
- 暫停：Resume、Setting、Restart、Exit。
- 設定：Language、Fullscreen。

## 對應文件
- [08_UIUX](../08_UIUX.md)

## 實作步驟
1. 建立 Home / Pause / Settings 元件。
2. 串接 `useSettingsStore`。
3. 實作 Fullscreen 與語言切換。
4. 套用淡藍天空背景、半透明淺色面板與莫蘭迪狀態色。

## 驗收標準
- 三個選單皆可運作並套用設定。
- Home、Pause、Settings 的背景、按鈕、面板與文字符合淡藍天空與莫蘭迪色系規範。

## 相依任務
- 003

## 目前盤點（2026-07-10）

### 已實作
- `src/routes/HomePage.tsx`：Start → `/game`、**Continue**（`hasContinuableProgress`）、Gallery、Settings 連結；`AchievementPanel`。
- `src/components/PauseMenu.tsx` + `GamePage`：Esc 暫停／恢復、`restartChapter` 重開本章；暫停時隱藏觸控按鈕。
- `src/routes/SettingsPage.tsx`：Language select、Fullscreen 切換。
- `src/stores/useSettingsStore.ts`：`language`、`fullscreen`；`toggleFullscreen()` 呼叫 Fullscreen API；LocalStorage persist。
- `src/hooks/useFullscreenSync.ts`：同步瀏覽器 fullscreen 狀態。
- `src/components/AppNav.tsx`：桌機全站導覽（手機遊戲頁隱藏）。
- 手機斷點（820px）下 Home / Pause / Settings / Dialogue 樣式已調整（見 `styles.css`、`08_UIUX.md`）。

### 尚未實作
- **i18n**：`language` store 存在，但 UI 文案全為硬編碼繁中；`SettingsPage` 僅 `zh-Hant` 選項且 `onChange` 固定設 `zh-Hant`。
- Pause 選單內尚無直接進入 Settings 的捷徑（需離開遊戲後由導覽或首頁進入）。
