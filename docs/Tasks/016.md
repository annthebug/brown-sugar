# Task 016 — UI：首頁 / 暫停 / 設定

狀態：🟨 進行中

## 目標
建立主要選單 UI。

## 範圍
- 首頁：Start、Continue、Gallery、Setting。
- 暫停：Resume、Setting、Restart、Exit。
- 設定：Sound、Language、Fullscreen。

## 對應文件
- [08_UIUX](../08_UIUX.md)

## 實作步驟
1. 建立 Home / Pause / Settings 元件。
2. 串接 `useSettingsStore`。
3. 實作 Fullscreen 與語言切換。
4. 套用淡藍天空背景、半透明淺色面板與莫蘭迪狀態色。

## 驗收標準
- 三個選單皆可運作並套用設定。
- Home、Pause、Settings 的背景、按鈕、面板與文字符合淡藍天空與莫蘭迪色系規範。

## 相依任務
- 003, 015

## 目前盤點（2026-07-09）

### 已實作
- `src/routes/HomePage.tsx`：淡藍天空 + 莫蘭迪 `hero-card` / `journey-panel`（`src/styles.css`）；**Start** → `/game`、**Gallery** → `/gallery`。
- `src/routes/SettingsPage.tsx`：Sound 滑桿（0–100）、Language select（`zh-Hant` / `en`）、Fullscreen 切換按鈕。
- `src/stores/useSettingsStore.ts`：`soundVolume`、`language`、`fullscreen`；LocalStorage persist（`perfect-bowl-settings`）。
- `src/components/AppNav.tsx`：全站導覽含 Home / Game / Gallery / Settings / Ending。
- 全站視覺已套用淡藍漸層天空、半透明面板與莫蘭迪按鈕色。

### 尚未實作
- 首頁 **Continue** 按鈕 **無**（僅 Start + Gallery）。
- 首頁 **Setting** 捷徑 **無**（需經 `AppNav` 進入設定頁）。
- **Pause 選單**（Resume / Setting / Restart / Exit）**完全不存在**；`src/` 無 Pause 元件或 ESC 暫停處理；`GamePage` 僅有回 Home 的 `Link`。
- 實際 **Fullscreen API**（`requestFullscreen` 等）**無**；`toggleFullscreen()` 僅切換 store 布林 preference。
- Sound **已串接 Howler.js** SFX（Task 015 部分完成）；Language 尚無 i18n 文案套用。
- Continue / Restart 遊戲流程 **無**（依賴 Task 018 存檔系統）。
