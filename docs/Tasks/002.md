# Task 002 — 整合 Phaser 3 與 React

狀態：✅ 完成

## 目標
在 React 專案中掛載 Phaser 3 遊戲畫布，並建立 React 與 Phaser 溝通機制。

## 範圍
- 安裝 Phaser 3。
- 建立 `game/config.ts` 與初始 Scene。
- 於 `/game` 路由掛載 Phaser 畫布。
- 建立事件匯流排（event bus）供雙向溝通。

## 對應文件
- [11_TechnicalArchitecture](../11_TechnicalArchitecture.md)

## 實作步驟
1. 安裝 `phaser`。
2. 建立 `BootScene` 與 `PreloadScene`。
3. 於 React 元件中初始化 / 銷毀 Phaser game 實例。
4. 建立 event bus（例如自訂 EventEmitter）連接 Zustand。

## 驗收標準
- 進入 `/game` 可看到 Phaser 畫布運作。
- Phaser 可送出事件被 React 接收。

## 相依任務
- 001

## 目前盤點（2026-07-08）
- React 元件已可在 `/game` 初始化並銷毀 Phaser game 實例。
- 已建立 `BootScene`、`PreloadScene`、`GameScene` 與 typed event bus，Phaser 事件可更新 React 狀態。
- `package.json` 與 lockfile 已由 Phaser 4 修正為 Phaser 3（`phaser@^3.90.0`），與技術文件一致。
