# Task 021 — PWA 設定

狀態：✅ 完成

## 目標
讓遊戲可安裝為 PWA。

## 範圍
- manifest 與 service worker。
- 啟動圖示、載入畫面。
- 資源快取（離線啟動）。

## 對應文件
- [08_UIUX](../08_UIUX.md)
- [13_Deployment](../13_Deployment.md)

## 實作步驟
1. ✅ 導入 Vite PWA 外掛（`vite-plugin-pwa`）。
2. ✅ 設定 manifest 與圖示（`public/icons/` 192 / 512 / apple-touch-icon）。
3. ✅ 設定 Workbox 預快取、`navigateFallback` 與圖片 runtime 快取；`autoUpdate` 版本失效。

## 驗收標準
- ✅ 可安裝至主畫面（Android Chrome / iOS Safari）。
- ✅ 離線可開啟首頁（預快取 `index.html` + 主要 bundle）。

## 相依任務
- 001

## 實作備註
- `vite.config.ts`：`VitePWA` manifest、`workbox.maximumFileSizeToCacheInBytes` 3MB（Phaser bundle 較大）。
- `index.html`：`theme-color`、`viewport-fit=cover`、`apple-touch-icon`。
- 建置產物：`dist/manifest.webmanifest`、`dist/sw.js`、`dist/registerSW.js`。
