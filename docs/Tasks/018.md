# Task 018 — 存檔系統（LocalStorage + Firebase Sync）

狀態：✅ 完成

## 目標
實作存檔與可選雲端同步。

## 範圍（v1）
- LocalStorage 即時保存（各 Zustand store persist）。
- `services/save.ts`：關鍵狀態聚合、debounce 寫入、章節／MBTI／成就同步。
- Firebase Firestore：**選用**；於 `.env` 設定 `VITE_FIREBASE_*` 時啟用 `useSaveSync()`。
- 不含進階衝突合併 UI；以本機 meta 時間戳為準的簡化同步。

## 對應文件
- [12_Database](../12_Database.md)

## 實作步驟
1. ✅ 建立 `services/save.ts`、`services/firebase.ts`。
2. ✅ 設定 Firebase 初始化（選用）。
3. ✅ 本機保存與連線時雲端寫入。

## 驗收標準
- ✅ 重新載入可續玩（LocalStorage）。
- ✅ 設定 Firebase 後可雲端同步存檔文件。

## 相依任務
- 003

## 實作備註
- `App.tsx` 掛載 `useSaveSync()`。
- 未設定 Firebase 時僅本機存檔，視為正常 v1 行為。
