# Task 018 — 存檔系統（LocalStorage + Firebase Sync）

狀態：🟨 進行中

## 目標
實作存檔與雲端同步。

## 範圍
- Auto Save（關鍵節點）。
- LocalStorage 即時保存。
- Firebase Firestore 同步。

## 對應文件
- [12_Database](../12_Database.md)

## 實作步驟
1. 建立 `services/save.ts`。
2. 設定 Firebase 專案與初始化。
3. 實作本機保存與雲端同步（含衝突處理）。

## 驗收標準
- 進度可本機保存並雲端同步。
- 重新載入可續玩。

## 相依任務
- 003

## 目前盤點（2026-07-08）
- `useGameStore`、`useSettingsStore`、`useGalleryStore`、`useMbtiStore` 已透過 Zustand persist 寫入 LocalStorage。
- 尚未建立 `services/save.ts`、Firebase 初始化、Firestore 同步、關鍵節點 auto save 與衝突處理。
- 目前僅完成本機狀態保存基礎，尚未達「LocalStorage + Firebase Sync」完整驗收。
