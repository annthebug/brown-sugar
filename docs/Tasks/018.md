# Task 018 — 存檔系統（LocalStorage + Firebase Sync）

狀態：✅ 完成

## 目標
實作存檔與雲端同步。

## 範圍
- Auto Save（關鍵節點）。
- LocalStorage 即時保存。
- Firebase Firestore 同步。

## 對應文件
- [12_Database](../12_Database.md)

## 實作步驟
1. 建立 `services/save.ts`。
2. 設定 Firebase 專案與初始化。
3. 實作本機保存與雲端同步（含衝突處理）。

## 驗收標準
- 進度可本機保存並雲端同步。
- 重新載入可續玩。

## 相依任務
- 003

## 目前盤點（2026-07-10）

### 已實作
- `useGameStore`、`useSettingsStore`、`useGalleryStore`、`useMbtiStore`、`useAchievementStore` 皆透過 Zustand persist 寫入 LocalStorage。
- `src/services/save.ts`：`useSaveSync`、`buildSaveDocument`、debounce 自動存檔、Firestore 讀寫、`updatedAt` 衝突處理；成就與 MBTI 子文件同步。
- `src/services/firebase.ts`：Firestore 初始化；`isFirebaseConfigured()` 檢查 `VITE_FIREBASE_*`。
- `src/App.tsx` 掛載 `useSaveSync()`。
- `PreloadScene` 讀取 persisted 進度決定啟動章節。
- `.env.example` 列出 Firebase 環境變數。

### 備註
- Firebase 為**選用**：未設定環境變數時僅使用 LocalStorage，不影響本機遊玩與 Continue。
- 雲端同步需部署環境自行設定 Firebase 專案與 Vercel env。
