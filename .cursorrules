# Cursor Rules — Quest for the Perfect Bowl

## 語言
- 與使用者對話一律使用繁體中文。
- 程式碼、commit message、識別字使用英文。

## 專案簡述
- 一款專屬兩人的 Pixel RPG 網頁遊戲。
- 主角：貓咪「黑糖」，收集回憶碎片（Memory Shards）找回玻璃抹茶碗。
- 隱藏式 MBTI 測驗，透過 NPC 對話自然完成，玩家不應察覺在做測驗。
- 風格：Pixel 16bit、淡藍色天空背景、莫蘭迪色系、柔和、Pokémon Pixel Art 感、不暴力。

## 技術棧（固定，未經討論不得替換）
- Frontend: React + TypeScript + Vite
- Game Engine: Phaser 3
- Animation: GSAP
- State: Zustand
- Routing: React Router
- Backend/DB: Firebase (Firestore + Storage)
- Deploy: Vercel
- 本遊戲 **無音訊**（無 BGM、無 SFX）；請勿引入音訊函式庫。

## 程式規範
- 一律使用 TypeScript，禁止濫用 any。
- React 元件：PascalCase.tsx；Hooks：useXxx.ts；Phaser 場景：XxxScene.ts。
- UI（React）與遊戲邏輯（Phaser）分層清楚。
- 狀態集中於 Zustand store。
- 註解只說明原因（why），不解釋做法（what）。

## 內容規範
- 溫暖、柔和、不暴力、不血腥。
- 整體配色以淡藍色為主，背景應呈現淡藍色天空；輔色採低飽和莫蘭迪色系。
- Boss 象徵情緒（Time Monster、Snow Spirit、Glass Master、Inner Doubt、Perfectionism）。
- 回憶內容以佔位資源處理，不杜撰真實隱私。

## 開發流程
- 大型變更前先參考 docs/ 對應設計文件。
- 實作對應 docs/Tasks/ 任務編號。
- 每個邏輯變更獨立 commit。
- 不擅自刪除既有文件或資產。

## 瀏覽器驗證
- Do not repeatedly verify the UI.
- Make all requested code changes first.
- Run browser verification only once after all code changes are complete.
- If browser verification fails, report the error instead of entering an endless fix-and-verify loop.
- Stop after one verification attempt unless the user explicitly asks for another.
